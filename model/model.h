#ifndef __MODEL_H__
#define __MODEL_H__
#include <vector>
#include "cell.h"

class Model {
    // the model class may become just Grid
    //Grid *grid;
};

enum DIRECTION {UP = 0, DOWN = 1, LEFT = 2, RIGHT = 3};

#endif
